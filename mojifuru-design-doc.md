# もじふる 設計まとめ(PoC用)

## 1. コンセプト

上からランダムに降ってくるひらがな文字をタップして集め、制限時間内に単語を作ってスコアを競うゲーム。短い単語を複数作る戦略と、長い単語でボーナスを狙う戦略の両方が成立するように設計する。

- アプリ名表記:**もじふる**(全編ひらがな。ロゴ・画面表示ともに漢字は使わない)
- ターゲット体験:タイパ良く一人でサクッと遊べる/友達とリンク一つで対戦できる、の両対応
- バックエンドなし:GitHub Pages(静的ホスティング)+ Firebase RTDB(Anonymous Auth)のみで完結させる

## 2. デザイン仕様(確定)

- 配色:パステル系3色(マゼンタ/オレンジ/アクア)を文字チップにランダム割当
- 文字チップ:丸型、ポップで親しみやすい印象を優先(和風・落ち着いたトーンは不採用)
- UIコピー:ひらがな中心の平易な文言(例:「この単語で確定」)
- ボーナス表示バッジ:画面上部に常時表示、警告色系(黄〜アンバー)
- レイアウト:モバイルファースト、ゲームキャンバス最大幅 420px 固定(PCでは左右レターボックスで公平性を担保)
- 開発者ツール対策:F12無効化などは実効性が低いため不採用。右クリック無効化程度の軽い抑止のみ任意で可

## 3. 画面構成

1. タイトル画面:「ひとりで遊ぶ」「友達と対戦(ルーム作成/参加)」
2. ゲーム画面:落下する文字 + 構成中の単語バー + 確定ボタン + 残り時間 + ボーナス条件バッジ
3. 結果画面:スコア内訳(単語数・ボーナス達成語) + ランキング投稿
4. リーダーボード画面:全体ランキング/(対戦時)ルーム内リアルタイム順位
5. 有効単語一覧画面:文字数タブ + 50音インデックス + フリーワード検索(仮想スクロール表示)
6. ライセンス表記ページ:使用辞書データの出典・ライセンス・改変内容を明記

## 4. 辞書と単語判定

### データソース
- **Mozc辞書**(Google日本語入力オープンソース版、BSDライセンス):読み・単語・品詞のペアが揃っており、これを主軸にする
- **JMdict**(CC BY-SA系ライセンス):一般語彙のクロスチェック用途

### 抽出方針
- 品詞フィルタで一般名詞・形容詞・動詞の基本形に絞り、固有名詞・専門用語は除外
- 対象文字数:2〜8文字(かな読みベース)。短すぎる範囲に絞ると長い単語のボーナスが成立しないため、範囲を広めに取る
- ビルド時にスクリプトで「判定用DAWG」と「一覧表示用フラットJSON(`{word, kana, length}[]`)」の2種類を生成

### 判定漏れへの対処
- 辞書にない単語は「エラー」ではなく「未登録(0点、ペナルティなし)」として処理し、プレイを止めない
- 未登録単語は `/wordCandidates/{word}` に収集し、頻出するものは次回リリースで辞書に追加(再デプロイのみで運用可能)

## 5. スコアリング

- 短い単語を複数成立させると加点(単語ごとに得点)
- 5文字以上でボーナス、7文字以上で大ボーナス(段階制)
- 長い単語を組み立てる猶予として、降下速度や一時ストックスロットの調整を検討

## 6. 技術構成

### プロジェクト構成
```
mojifuru/
├── src/
│   ├── game/
│   │   ├── fallingLetters.ts     # 文字の生成・降下ロジック(純粋関数中心)
│   │   ├── wordValidator.ts      # DAWG検索による判定
│   │   └── scoring.ts            # 得点計算(短語×複数、規定文字数ボーナス)
│   ├── firebase/
│   │   ├── config.ts
│   │   ├── room.ts               # ルーム作成/参加/シード共有
│   │   └── leaderboard.ts        # スコア書き込み・取得
│   ├── components/               # React UI(タイトル/ゲーム/結果/一覧/ライセンス)
│   └── App.tsx
├── scripts/
│   └── buildDict.ts              # Mozc辞書→フィルタ→DAWG+一覧JSON変換(ビルド時実行)
├── public/
│   ├── dict.dawg                 # ビルド済み判定用辞書
│   └── wordlist.json             # 一覧表示用データ
└── firebase.rules.json
```

### RTDB構成

**ソロプレイ用リーダーボード**
```
/leaderboard/{uid}: { name, bestScore, updatedAt }
```

**対戦モード用ルーム**
```
/rooms/{roomId}
  ├─ seed: <number>              # 全員共通の文字降下パターン用シード
  ├─ startAt: <serverTimestamp>
  ├─ duration: 60
  ├─ players/{uid}: { name, score, wordsFormed: [] }
  └─ takenLetters/{letterId}: uid   # 誰がどの文字を取ったか(取り合いの同期・早い者勝ち)
```

**未登録単語収集**
```
/wordCandidates/{word}: { count, firstSeenAt }
```

### セキュリティルールの要点
- `players/{uid}/score` は本人のuidのときのみ書き込み可、かつ1回の増加量に上限を設定
- `takenLetters/{letterId}` は「まだ誰も取っていない(値がnull)ときのみ書き込み可」で疑似排他制御
- `playerInfo/{uid}`(将来モードで個人情報を持たせる場合)は `auth.uid == $uid` のときのみ読める設定にする

## 7. ライセンス表記(必須対応)

- Mozc辞書(BSDライセンス)、JMdict(CC BY-SA)の出典・ライセンス文へのリンク・改変内容(読みと品詞を抽出しフィルタリングして使用、等)を `/license` ページに明記
- タイトル画面のメニューまたはフッターから常時アクセス可能にする

## 8. 未確定・今後詰める余地がある項目

- 文字チップの色配分パターンの微調整
- 降下速度・出現から消滅までの時間のバランス調整(長い単語を作る猶予の確保)
- 一時ストックスロットを導入するかどうか
- 対戦モードのマッチング導線(ルームコード共有のUI詳細)
