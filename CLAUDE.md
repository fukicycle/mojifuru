# CLAUDE.md

このファイルはClaude Codeがこのリポジトリで作業する際のガイドです。詳細な設計背景は `mojifuru-design-doc.md` を参照してください(このファイルには実装に必要な要点のみをまとめています)。

## プロジェクト概要

**もじふる**(全編ひらがな表記、漢字は使わない)は、上から降ってくるひらがな文字をタップして集め、制限時間内に単語を作ってスコアを競うWebゲーム。バックエンドサーバーは持たず、以下の構成で完結させる。

- フロントエンド:React + TypeScript
- ホスティング:GitHub Pages(静的ホスティング)
- リアルタイム同期・データ永続化:Firebase Realtime Database(Anonymous Auth)
- サーバーサイドロジックは一切持たない(Cloud Functions等も使わない前提)

## 現在のステータス

設計フェーズ完了、実装未着手。このリポジトリはPoCとして一から実装を始める段階。

## 技術的な絶対制約

- **バックエンドサーバーなし**。単語判定・得点計算などはすべてクライアント側の純粋関数で完結させること
- **辞書ファイルは静的アセット**として `public/` に同梱する(外部APIへの都度問い合わせはしない)
- 対戦モードの同期はFirebase RTDBのみで行う(WebSocketサーバー等の別途構築はしない)

## ディレクトリ構成(この構成に従うこと)

```
mojifuru/
├── src/
│   ├── game/
│   │   ├── fallingLetters.ts     # 文字の生成・降下ロジック(純粋関数)
│   │   ├── wordValidator.ts      # DAWG検索による単語判定
│   │   └── scoring.ts            # 得点計算
│   ├── firebase/
│   │   ├── config.ts
│   │   ├── room.ts               # ルーム作成/参加/シード共有
│   │   └── leaderboard.ts        # スコア書き込み・取得
│   ├── components/               # タイトル/ゲーム/結果/一覧/ライセンス各画面
│   └── App.tsx
├── scripts/
│   └── buildDict.ts              # Mozc辞書→フィルタ→DAWG+一覧JSON変換(ビルド時実行)
├── public/
│   ├── dict.dawg                 # 判定用辞書(ビルド生成物)
│   └── wordlist.json             # 一覧表示用データ(ビルド生成物)
└── firebase.rules.json
```

## ゲームルールの仕様(要点)

- 降ってくる文字はひらがなのみ(漢字・カタカナは扱わない)
- 文字数2〜8文字の範囲で単語成立を判定する(短すぎる範囲に絞ると長い単語のボーナスが成立しないため必須)
- 得点:短い単語を複数成立させると加点。5文字以上でボーナス、7文字以上で大ボーナスの段階制
- 辞書にない単語は「エラー」ではなく「未登録・0点」として処理し、プレイを止めない
- 未登録単語は `/wordCandidates/{word}` (RTDB) に収集し、後から辞書を育てる運用にする

## 辞書データ

- 主データ:Mozc辞書(Google日本語入力オープンソース版、BSDライセンス)から読み・単語・品詞を抽出
- 補助データ:JMdict(CC BY-SA系ライセンス)でクロスチェック
- 抽出対象:名詞(一般・サ変接続・形容動詞語幹・副詞可能)、形容詞・動詞の基本形(固有名詞・数詞・代名詞・専門用語は除外)
- ビルド時に2種類のファイルを生成すること:
  - 判定用:DAWG形式(`public/dict.dawg`)
  - 一覧表示用:フラットJSON配列 `{word, kana, length}[]`(`public/wordlist.json`)
- **ライセンス表記ページ(`/license`)は必須実装**。使用データの出典・ライセンス・改変内容を明記すること

## RTDBスキーマ

```
/leaderboard/{uid}: { name, bestScore, updatedAt }

/rooms/{roomId}
  ├─ seed: <number>
  ├─ startAt: <serverTimestamp>
  ├─ duration: 60
  ├─ players/{uid}: { name, score, wordsFormed: [] }
  └─ takenLetters/{letterId}: uid

/wordCandidates/{word}: { count, firstSeenAt }
```

## セキュリティルールの必須要件

- `players/{uid}/score`:本人のuidのときのみ書き込み可、かつ1回の増加量に上限を設定(スコア改ざん対策)
- `takenLetters/{letterId}`:値がnullのときのみ書き込み可(早い者勝ちの排他制御)
- 個人情報を持たせるフィールドがある場合は `auth.uid == $uid` のときのみ読める設定にする

## デザイン仕様(確定事項・変更しないこと)

- 配色:パステル系3色(マゼンタ/オレンジ/アクア)を文字チップにランダム割当
- 文字チップ:丸型、ポップで親しみやすい印象(和風・落ち着いたトーンは不採用と決定済み)
- UIコピー:ひらがな中心の平易な文言(例:「この単語で確定」)
- ボーナス表示バッジ:画面上部に常時表示、警告色系(黄〜アンバー)
- レイアウト:モバイルファースト。ゲームキャンバス最大幅420px固定、PCでは左右レターボックスで公平性を担保(大画面ほど有利にならないようにする意図)
- 開発者ツール対策:F12無効化などの強い制限は不採用(実効性が低いため)。右クリック無効化程度の軽い抑止のみ任意

## 明示的に不採用となった案(再検討不要)

- アプリ名の漢字表記(「文字降る」等) → 不採用、全編ひらがな「もじふる」で確定
- 和風テイストのビジュアルデザイン(青海波模様、朱色の印鑑ボタン等) → 一度試作したが不採用。誰でも気軽に遊べるポップな方向に確定済み
- F12/開発者ツールの無効化による対策 → 技術的に実効性がないため不採用

## 未確定・実装しながら詰める項目

- 文字チップの色配分パターンの微調整
- 降下速度・出現から消滅までの時間のバランス(長い単語を作る猶予の確保)
- 一時ストックスロット(構成中の単語を保留できる仕組み)を導入するかどうか
- 対戦モードのルームコード共有UIの詳細
